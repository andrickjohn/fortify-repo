---
description: Sync changes to Git
---

1. Check status
   // turbo
   `git status`

2. Stage all changes
   // turbo
   `git add .`

3. Commit changes (Prompt for message if not provided)
   `git commit -m "Syncing progress"`

4. Push to remote
   // turbo
   `git push`
